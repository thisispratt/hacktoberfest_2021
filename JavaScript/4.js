//Program to Multiply two Floating Point Numbers

var firstNum = prompt("Enter first number");
var secondNum = prompt("Enter second number");

var product = Number(firstNum) * Number(secondNum);

alert(`Product is: ${product}`);